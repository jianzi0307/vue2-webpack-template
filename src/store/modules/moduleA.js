// const INIT_ALIST = 'INIT_ALIST';

/**
 * A模块数据管理
 */
const state = {
  // aList: []
};

const actions = {
  // setAList({ commit }, params) {
  //   commit(INIT_ALIST, params);
  // }
};

const mutations = {
  // [INIT_ALIST]: (state, params) => {
  //   state.aList = params
  // },
};

export default {
  namespaced: true,
  state,
  actions,
  mutations,
};
