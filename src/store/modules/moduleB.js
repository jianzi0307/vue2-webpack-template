// const INIT_BLIST = 'INIT_BLIST';

/**
 * B模块数据管理
 */
const state = {
  // bList: []
};

const actions = {
  // setBList({ commit }, params) {
  //   commit(INIT_BLIST, params);
  // }
};

const mutations = {
  // [INIT_BLIST]: (state, params) => {
  //   state.bList = params
  // },
};

export default {
  namespaced: true,
  state,
  actions,
  mutations,
};
